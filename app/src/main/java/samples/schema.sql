CREATE TABLE `EMPLOYEE` (
	`EMPLOYEE_PK`	VARCHAR(20)	NOT NULL,
	`EMP_NAME`	VARCHAR(20)	NULL
);

CREATE TABLE `STATUS` (
	`STATUS_PK`	VARCHAR(20)	NOT NULL,
	`STATUS`	VARCHAR(20)	NULL
);

CREATE TABLE `DEPARTMENT` (
	`DEPARTMENT_PK`	VARCHAR(20)	NOT NULL,
	`DEP_NAME`	VARCHAR(20)	NULL
);

CREATE TABLE `ATTENDANCE` (
	`ATTENDANCE_PK`	VARCHAR(20)	NOT NULL,
	`EMPLOYEE_PK`	VARCHAR(20)	NOT NULL,
	`STATUS_PK`	VARCHAR(20)	NOT NULL,
	`DATE`	VARCHAR(20)	NULL
);

CREATE TABLE `EMP_DEP` (
	`EMP_DEP_PK`	VARCHAR(20)	NOT NULL,
	`EMPLOYEE_PK`	VARCHAR(20)	NOT NULL,
	`DEPARTMENT_PK`	VARCHAR(20)	NOT NULL
);

ALTER TABLE `EMPLOYEE` ADD CONSTRAINT `PK_EMPLOYEE` PRIMARY KEY (
	`EMPLOYEE_PK`
);

ALTER TABLE `STATUS` ADD CONSTRAINT `PK_STATUS` PRIMARY KEY (
	`STATUS_PK`
);

ALTER TABLE `DEPARTMENT` ADD CONSTRAINT `PK_DEPARTMENT` PRIMARY KEY (
	`DEPARTMENT_PK`
);

ALTER TABLE `ATTENDANCE` ADD CONSTRAINT `PK_ATTENDANCE` PRIMARY KEY (
	`ATTENDANCE_PK`,
	`EMPLOYEE_PK`,
	`STATUS_PK`
);

ALTER TABLE `EMP_DEP` ADD CONSTRAINT `PK_EMP_DEP` PRIMARY KEY (
	`EMP_DEP_PK`,
	`EMPLOYEE_PK`,
	`DEPARTMENT_PK`
);

ALTER TABLE `ATTENDANCE` ADD CONSTRAINT `FK_EMPLOYEE_TO_ATTENDANCE_1` FOREIGN KEY (
	`EMPLOYEE_PK`
)
REFERENCES `EMPLOYEE` (
	`EMPLOYEE_PK`
);

ALTER TABLE `ATTENDANCE` ADD CONSTRAINT `FK_STATUS_TO_ATTENDANCE_1` FOREIGN KEY (
	`STATUS_PK`
)
REFERENCES `STATUS` (
	`STATUS_PK`
);

ALTER TABLE `EMP_DEP` ADD CONSTRAINT `FK_EMPLOYEE_TO_EMP_DEP_1` FOREIGN KEY (
	`EMPLOYEE_PK`
)
REFERENCES `EMPLOYEE` (
	`EMPLOYEE_PK`
);

ALTER TABLE `EMP_DEP` ADD CONSTRAINT `FK_DEPARTMENT_TO_EMP_DEP_1` FOREIGN KEY (
	`DEPARTMENT_PK`
)
REFERENCES `DEPARTMENT` (
	`DEPARTMENT_PK`
);
